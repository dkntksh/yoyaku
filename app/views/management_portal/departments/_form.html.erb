<%= form_with(model: management_portal_department, local: true) do |form| %>
  <% if management_portal_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(management_portal_department.errors.count, "error") %> prohibited this management_portal_department from being saved:</h2>

      <ul>
        <% management_portal_department.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
